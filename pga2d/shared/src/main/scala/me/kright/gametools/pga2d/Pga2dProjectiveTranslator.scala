package me.kright.gametools.pga2d

import scala.annotation.targetName
import me.kright.gametools.flatarray.FlatDoubleSerializer


/**
 * A translator with an explicit (not necessarily 1.0) scalar part: an unnormalized version of Pga2dTranslator.
 *
 * Variable fields: s, wx, wy.
 *
 * Generated by pgaNdCodeGen (me.kright.gametools.pga.codegen.scalagen.pga2d.runScalaCodeGen).
 * Do not edit by hand: change the generator and re-run it.
 */
final case class Pga2dProjectiveTranslator(s: Double = 0.0,
                                           wx: Double = 0.0,
                                           wy: Double = 0.0) derives CanEqual, FlatDoubleSerializer:

  override def toString: String =
    s"Pga2dProjectiveTranslator(s = $s, wx = $wx, wy = $wy)"

  def dual: Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = 0.0,
      x = -wy,
      y = wx,
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = s,
    )

  def weight: Pga2dVector =
    Pga2dVector(
      x = -wy,
      y = wx,
    )

  def bulk: Double =
    s

  @targetName("unaryMinus")
  def unary_- : Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = -s,
      wx = -wx,
      wy = -wy,
    )

  def reverse: Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = s,
      wx = -wx,
      wy = -wy,
    )

  def antiReverse: Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = -s,
      wx = wx,
      wy = wy,
    )

  def bulkNormSquare: Double =
    s * s

  def bulkNorm: Double =
    Math.sqrt(bulkNormSquare)

  def normalizedByBulk =
    this / bulkNorm

  def weightNormSquare: Double =
    (wx * wx + wy * wy)

  def weightNorm: Double =
    Math.sqrt(weightNormSquare)

  def normalizedByWeight =
    this / weightNorm

  def normSquare: Double =
    (s * s + wx * wx + wy * wy)

  def norm: Double =
    Math.sqrt(normSquare)

  def normalizedByNorm =
    this / norm

  @targetName("times")
  def *(r: Double): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = r * s,
      wx = r * wx,
      wy = r * wy,
    )

  /** multiplies by the reciprocal: one division instead of one per component, at the cost of one extra rounding (~0.5 ulp) */
  @targetName("div")
  def /(r: Double): Pga2dProjectiveTranslator =
    this * (1.0 / r)

  @targetName("plus")
  def +(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = (r.s + s),
      wx = (r.wx + wx),
      wy = (r.wy + wy),
    )

  @targetName("minus")
  def -(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = (s - r.s),
      wx = (wx - r.wx),
      wy = (wy - r.wy),
    )

  def madd(r: Pga2dProjectiveTranslator, mult: Double): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = (s + mult * r.s),
      wx = (wx + mult * r.wx),
      wy = (wy + mult * r.wy),
    )

  /** componentwise product on the raw fields; NOT a geometric-algebra operation (basis-dependent) */
  def scale(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = s * r.s,
      wx = wx * r.wx,
      wy = wy * r.wy,
    )

  /** componentwise 1/x on the raw fields (a zero field yields Infinity); NOT the multiplicative inverse */
  def reciprocal: Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = 1.0 / s,
      wx = 1.0 / wx,
      wy = 1.0 / wy,
    )

  def toMultivector: Pga2dMultivector =
    Pga2dMultivector(
      s = s,
      w = 0.0,
      x = 0.0,
      y = 0.0,
      wx = wx,
      wy = wy,
      xy = 0.0,
      i = 0.0,
    )

  def toMotor: Pga2dMotor =
    Pga2dMotor(
      s = s,
      wx = wx,
      wy = wy,
      xy = 0.0,
    )

  def toProjectivePointUnsafe: Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = -wy,
      y = wx,
      w = 0.0,
    )

  def toRotorUnsafe: Pga2dRotor =
    Pga2dRotor(
      s = s,
      xy = 0.0,
    )

  def toTranslatorUnsafe: Pga2dTranslator =
    Pga2dTranslator(
      wx = wx,
      wy = wy,
    )

  def toVectorUnsafe: Pga2dVector =
    Pga2dVector(
      x = -wy,
      y = wx,
    )

  def toPointUnsafe: Pga2dPoint =
    Pga2dPoint(
      x = -wy,
      y = wx,
    )

  /** divides by the scalar part; s == 0 yields infinite or NaN components */
  def toTranslator: Pga2dTranslator =
    val mult = (1.0 / s)
    Pga2dTranslator(
      wx = mult * wx,
      wy = mult * wy,
    )

  infix def geometric(r: Pga2dMotor): Pga2dMotor =
    Pga2dMotor(
      s = r.s * s,
      wx = (r.s * wx + r.wx * s - r.xy * wy),
      wy = (r.s * wy + r.wy * s + r.xy * wx),
      xy = r.xy * s,
    )

  infix def geometric(r: Pga2dLine): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = (r.w * s + r.x * wx + r.y * wy),
      x = r.x * s,
      y = r.y * s,
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  infix def geometric(r: Pga2dProjectivePoint): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = (r.x * s - r.w * wx),
      y = (r.y * s - r.w * wy),
      w = r.w * s,
    )

  infix def geometric(r: Pga2dRotor): Pga2dMotor =
    Pga2dMotor(
      s = r.s * s,
      wx = (r.s * wx - r.xy * wy),
      wy = (r.s * wy + r.xy * wx),
      xy = r.xy * s,
    )

  infix def geometric(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = r.s * s,
      wx = (r.s * wx + r.wx * s),
      wy = (r.s * wy + r.wy * s),
    )

  infix def geometric(r: Pga2dTranslator): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = s,
      wx = (wx + r.wx * s),
      wy = (wy + r.wy * s),
    )

  infix def geometric(r: Pga2dVector): Pga2dVector =
    Pga2dVector(
      x = r.x * s,
      y = r.y * s,
    )

  infix def geometric(r: Pga2dPoint): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = (-wx + r.x * s),
      y = (-wy + r.y * s),
      w = s,
    )

  infix def geometric(r: Pga2dLineIdeal): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = (r.x * wx + r.y * wy),
      x = r.x * s,
      y = r.y * s,
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  infix def geometric(r: Pga2dPseudoScalar): Pga2dPseudoScalar =
    Pga2dPseudoScalar(
      i = r.i * s,
    )

  infix def geometric(r: Pga2dPointCenter.type): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = -wx,
      y = -wy,
      w = s,
    )

  infix def dot(r: Pga2dMotor): Pga2dMotor =
    Pga2dMotor(
      s = r.s * s,
      wx = (r.s * wx + r.wx * s),
      wy = (r.s * wy + r.wy * s),
      xy = r.xy * s,
    )

  infix def dot(r: Pga2dLine): Pga2dLine =
    Pga2dLine(
      x = r.x * s,
      y = r.y * s,
      w = (r.w * s + r.x * wx + r.y * wy),
    )

  infix def dot(r: Pga2dProjectivePoint): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = r.x * s,
      y = r.y * s,
      w = r.w * s,
    )

  infix def dot(r: Pga2dRotor): Pga2dMotor =
    Pga2dMotor(
      s = r.s * s,
      wx = r.s * wx,
      wy = r.s * wy,
      xy = r.xy * s,
    )

  infix def dot(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = r.s * s,
      wx = (r.s * wx + r.wx * s),
      wy = (r.s * wy + r.wy * s),
    )

  infix def dot(r: Pga2dTranslator): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = s,
      wx = (wx + r.wx * s),
      wy = (wy + r.wy * s),
    )

  infix def dot(r: Pga2dVector): Pga2dVector =
    Pga2dVector(
      x = r.x * s,
      y = r.y * s,
    )

  infix def dot(r: Pga2dPoint): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = r.x * s,
      y = r.y * s,
      w = s,
    )

  infix def dot(r: Pga2dLineIdeal): Pga2dLine =
    Pga2dLine(
      x = r.x * s,
      y = r.y * s,
      w = (r.x * wx + r.y * wy),
    )

  infix def dot(r: Pga2dPseudoScalar): Pga2dPseudoScalar =
    Pga2dPseudoScalar(
      i = r.i * s,
    )

  infix def dot(r: Pga2dPointCenter.type): Pga2dRotor =
    Pga2dRotor(
      s = 0.0,
      xy = s,
    )

  infix def wedge(r: Pga2dMotor): Pga2dMotor =
    Pga2dMotor(
      s = r.s * s,
      wx = (r.s * wx + r.wx * s),
      wy = (r.s * wy + r.wy * s),
      xy = r.xy * s,
    )

  inline infix def ^(r: Pga2dMotor): Pga2dMotor = wedge(r)

  inline infix def meet(r: Pga2dMotor): Pga2dMotor = wedge(r)

  infix def wedge(r: Pga2dLine): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = r.w * s,
      x = r.x * s,
      y = r.y * s,
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  inline infix def ^(r: Pga2dLine): Pga2dMultivector = wedge(r)

  inline infix def meet(r: Pga2dLine): Pga2dMultivector = wedge(r)

  infix def wedge(r: Pga2dProjectivePoint): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = r.x * s,
      y = r.y * s,
      w = r.w * s,
    )

  inline infix def ^(r: Pga2dProjectivePoint): Pga2dProjectivePoint = wedge(r)

  inline infix def meet(r: Pga2dProjectivePoint): Pga2dProjectivePoint = wedge(r)

  infix def wedge(r: Pga2dRotor): Pga2dMotor =
    Pga2dMotor(
      s = r.s * s,
      wx = r.s * wx,
      wy = r.s * wy,
      xy = r.xy * s,
    )

  inline infix def ^(r: Pga2dRotor): Pga2dMotor = wedge(r)

  inline infix def meet(r: Pga2dRotor): Pga2dMotor = wedge(r)

  infix def wedge(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = r.s * s,
      wx = (r.s * wx + r.wx * s),
      wy = (r.s * wy + r.wy * s),
    )

  inline infix def ^(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator = wedge(r)

  inline infix def meet(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator = wedge(r)

  infix def wedge(r: Pga2dTranslator): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = s,
      wx = (wx + r.wx * s),
      wy = (wy + r.wy * s),
    )

  inline infix def ^(r: Pga2dTranslator): Pga2dProjectiveTranslator = wedge(r)

  inline infix def meet(r: Pga2dTranslator): Pga2dProjectiveTranslator = wedge(r)

  infix def wedge(r: Pga2dVector): Pga2dVector =
    Pga2dVector(
      x = r.x * s,
      y = r.y * s,
    )

  inline infix def ^(r: Pga2dVector): Pga2dVector = wedge(r)

  inline infix def meet(r: Pga2dVector): Pga2dVector = wedge(r)

  infix def wedge(r: Pga2dPoint): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = r.x * s,
      y = r.y * s,
      w = s,
    )

  inline infix def ^(r: Pga2dPoint): Pga2dProjectivePoint = wedge(r)

  inline infix def meet(r: Pga2dPoint): Pga2dProjectivePoint = wedge(r)

  infix def wedge(r: Pga2dLineIdeal): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = 0.0,
      x = r.x * s,
      y = r.y * s,
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  inline infix def ^(r: Pga2dLineIdeal): Pga2dMultivector = wedge(r)

  inline infix def meet(r: Pga2dLineIdeal): Pga2dMultivector = wedge(r)

  infix def wedge(r: Pga2dPseudoScalar): Pga2dPseudoScalar =
    Pga2dPseudoScalar(
      i = r.i * s,
    )

  inline infix def ^(r: Pga2dPseudoScalar): Pga2dPseudoScalar = wedge(r)

  inline infix def meet(r: Pga2dPseudoScalar): Pga2dPseudoScalar = wedge(r)

  infix def wedge(r: Pga2dPointCenter.type): Pga2dRotor =
    Pga2dRotor(
      s = 0.0,
      xy = s,
    )

  inline infix def ^(r: Pga2dPointCenter.type): Pga2dRotor = wedge(r)

  inline infix def meet(r: Pga2dPointCenter.type): Pga2dRotor = wedge(r)

  infix def antiGeometric(r: Pga2dMotor): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = (r.wy * wx - r.wx * wy),
      x = (r.xy * wx - r.s * wy - r.wy * s),
      y = (r.s * wx + r.wx * s + r.xy * wy),
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.wx * wx + r.wy * wy),
    )

  infix def antiGeometric(r: Pga2dLine): Pga2dMotor =
    Pga2dMotor(
      s = (r.y * wx - r.x * wy),
      wx = -r.w * wy,
      wy = r.w * wx,
      xy = (r.x * wx + r.y * wy - r.w * s),
    )

  infix def antiGeometric(r: Pga2dProjectivePoint): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = (-r.x * wx - r.y * wy),
      x = (r.w * wx + r.x * s),
      y = (r.w * wy + r.y * s),
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  infix def antiGeometric(r: Pga2dRotor): Pga2dLineIdeal =
    Pga2dLineIdeal(
      x = (r.xy * wx - r.s * wy),
      y = (r.s * wx + r.xy * wy),
    )

  infix def antiGeometric(r: Pga2dProjectiveTranslator): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = (r.wy * wx - r.wx * wy),
      x = (-r.s * wy - r.wy * s),
      y = (r.s * wx + r.wx * s),
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.wx * wx + r.wy * wy),
    )

  infix def antiGeometric(r: Pga2dTranslator): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = (r.wy * wx - r.wx * wy),
      x = (-wy - r.wy * s),
      y = (wx + r.wx * s),
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.wx * wx + r.wy * wy),
    )

  infix def antiGeometric(r: Pga2dVector): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = (-r.x * wx - r.y * wy),
      x = r.x * s,
      y = r.y * s,
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  infix def antiGeometric(r: Pga2dPoint): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = (-r.x * wx - r.y * wy),
      x = (wx + r.x * s),
      y = (wy + r.y * s),
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  infix def antiGeometric(r: Pga2dLineIdeal): Pga2dRotor =
    Pga2dRotor(
      s = (r.y * wx - r.x * wy),
      xy = (r.x * wx + r.y * wy),
    )

  infix def antiGeometric(r: Pga2dPseudoScalar): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = r.i * s,
      wx = r.i * wx,
      wy = r.i * wy,
    )

  infix def antiGeometric(r: Pga2dPointCenter.type): Pga2dLineIdeal =
    Pga2dLineIdeal(
      x = wx,
      y = wy,
    )

  infix def antiDot(r: Pga2dMotor): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = 0.0,
      x = (-r.s * wy - r.wy * s),
      y = (r.s * wx + r.wx * s),
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.wx * wx + r.wy * wy),
    )

  infix def antiDot(r: Pga2dLine): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = -r.w * wx,
      y = -r.w * wy,
      w = (r.x * wx + r.y * wy - r.w * s),
    )

  infix def antiDot(r: Pga2dProjectivePoint): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = 0.0,
      x = r.x * s,
      y = r.y * s,
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  infix def antiDot(r: Pga2dRotor): Pga2dLineIdeal =
    Pga2dLineIdeal(
      x = -r.s * wy,
      y = r.s * wx,
    )

  infix def antiDot(r: Pga2dProjectiveTranslator): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = 0.0,
      x = (-r.s * wy - r.wy * s),
      y = (r.s * wx + r.wx * s),
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.wx * wx + r.wy * wy),
    )

  infix def antiDot(r: Pga2dTranslator): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = 0.0,
      x = (-wy - r.wy * s),
      y = (wx + r.wx * s),
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.wx * wx + r.wy * wy),
    )

  infix def antiDot(r: Pga2dVector): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = 0.0,
      x = r.x * s,
      y = r.y * s,
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  infix def antiDot(r: Pga2dPoint): Pga2dMultivector =
    Pga2dMultivector(
      s = 0.0,
      w = 0.0,
      x = r.x * s,
      y = r.y * s,
      wx = 0.0,
      wy = 0.0,
      xy = 0.0,
      i = (r.y * wx - r.x * wy),
    )

  infix def antiDot(r: Pga2dLineIdeal): Pga2dRotor =
    Pga2dRotor(
      s = 0.0,
      xy = (r.x * wx + r.y * wy),
    )

  infix def antiDot(r: Pga2dPseudoScalar): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = r.i * s,
      wx = r.i * wx,
      wy = r.i * wy,
    )

  infix def antiWedge(r: Pga2dMotor): Pga2dLine =
    Pga2dLine(
      x = r.xy * wx,
      y = r.xy * wy,
      w = (r.wy * wx - r.wx * wy),
    )

  inline infix def v(r: Pga2dMotor): Pga2dLine = antiWedge(r)

  inline infix def join(r: Pga2dMotor): Pga2dLine = antiWedge(r)

  infix def antiWedge(r: Pga2dLine): Double =
    (r.y * wx - r.x * wy)

  inline infix def v(r: Pga2dLine): Double = antiWedge(r)

  inline infix def join(r: Pga2dLine): Double = antiWedge(r)

  infix def antiWedge(r: Pga2dProjectivePoint): Pga2dLine =
    Pga2dLine(
      x = r.w * wx,
      y = r.w * wy,
      w = (-r.x * wx - r.y * wy),
    )

  inline infix def v(r: Pga2dProjectivePoint): Pga2dLine = antiWedge(r)

  inline infix def join(r: Pga2dProjectivePoint): Pga2dLine = antiWedge(r)

  infix def antiWedge(r: Pga2dRotor): Pga2dLineIdeal =
    Pga2dLineIdeal(
      x = r.xy * wx,
      y = r.xy * wy,
    )

  inline infix def v(r: Pga2dRotor): Pga2dLineIdeal = antiWedge(r)

  inline infix def join(r: Pga2dRotor): Pga2dLineIdeal = antiWedge(r)

  infix def antiWedge(r: Pga2dProjectiveTranslator): Pga2dLine =
    Pga2dLine(
      x = 0.0,
      y = 0.0,
      w = (r.wy * wx - r.wx * wy),
    )

  inline infix def v(r: Pga2dProjectiveTranslator): Pga2dLine = antiWedge(r)

  inline infix def join(r: Pga2dProjectiveTranslator): Pga2dLine = antiWedge(r)

  infix def antiWedge(r: Pga2dTranslator): Pga2dLine =
    Pga2dLine(
      x = 0.0,
      y = 0.0,
      w = (r.wy * wx - r.wx * wy),
    )

  inline infix def v(r: Pga2dTranslator): Pga2dLine = antiWedge(r)

  inline infix def join(r: Pga2dTranslator): Pga2dLine = antiWedge(r)

  infix def antiWedge(r: Pga2dVector): Pga2dLine =
    Pga2dLine(
      x = 0.0,
      y = 0.0,
      w = (-r.x * wx - r.y * wy),
    )

  inline infix def v(r: Pga2dVector): Pga2dLine = antiWedge(r)

  inline infix def join(r: Pga2dVector): Pga2dLine = antiWedge(r)

  infix def antiWedge(r: Pga2dPoint): Pga2dLine =
    Pga2dLine(
      x = wx,
      y = wy,
      w = (-r.x * wx - r.y * wy),
    )

  inline infix def v(r: Pga2dPoint): Pga2dLine = antiWedge(r)

  inline infix def join(r: Pga2dPoint): Pga2dLine = antiWedge(r)

  infix def antiWedge(r: Pga2dLineIdeal): Double =
    (r.y * wx - r.x * wy)

  inline infix def v(r: Pga2dLineIdeal): Double = antiWedge(r)

  inline infix def join(r: Pga2dLineIdeal): Double = antiWedge(r)

  infix def antiWedge(r: Pga2dPseudoScalar): Pga2dProjectiveTranslator =
    Pga2dProjectiveTranslator(
      s = r.i * s,
      wx = r.i * wx,
      wy = r.i * wy,
    )

  inline infix def v(r: Pga2dPseudoScalar): Pga2dProjectiveTranslator = antiWedge(r)

  inline infix def join(r: Pga2dPseudoScalar): Pga2dProjectiveTranslator = antiWedge(r)

  infix def antiWedge(r: Pga2dPointCenter.type): Pga2dLineIdeal =
    Pga2dLineIdeal(
      x = wx,
      y = wy,
    )

  inline infix def v(r: Pga2dPointCenter.type): Pga2dLineIdeal = antiWedge(r)

  inline infix def join(r: Pga2dPointCenter.type): Pga2dLineIdeal = antiWedge(r)

  infix def sandwich(r: Pga2dMotor): Pga2dMotor =
    val sMs = s * s
    Pga2dMotor(
      s = r.s * sMs,
      wx = (r.wx * sMs - 2.0 * r.xy * s * wy),
      wy = (r.wy * sMs + 2.0 * r.xy * s * wx),
      xy = r.xy * sMs,
    )

  infix def sandwich(r: Pga2dLine): Pga2dLine =
    val sMs = s * s
    Pga2dLine(
      x = r.x * sMs,
      y = r.y * sMs,
      w = (r.w * sMs + 2.0 * s * (r.x * wx + r.y * wy)),
    )

  infix def sandwich(r: Pga2dProjectivePoint): Pga2dProjectivePoint =
    val sMs = s * s
    Pga2dProjectivePoint(
      x = (r.x * sMs - 2.0 * r.w * s * wx),
      y = (r.y * sMs - 2.0 * r.w * s * wy),
      w = r.w * sMs,
    )

  infix def sandwich(r: Pga2dRotor): Pga2dMotor =
    val sMs = s * s
    Pga2dMotor(
      s = r.s * sMs,
      wx = -2.0 * r.xy * s * wy,
      wy = 2.0 * r.xy * s * wx,
      xy = r.xy * sMs,
    )

  infix def sandwich(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator =
    val sMs = s * s
    Pga2dProjectiveTranslator(
      s = r.s * sMs,
      wx = r.wx * sMs,
      wy = r.wy * sMs,
    )

  infix def sandwich(r: Pga2dTranslator): Pga2dProjectiveTranslator =
    val sMs = s * s
    Pga2dProjectiveTranslator(
      s = sMs,
      wx = r.wx * sMs,
      wy = r.wy * sMs,
    )

  infix def sandwich(r: Pga2dVector): Pga2dVector =
    val sMs = s * s
    Pga2dVector(
      x = r.x * sMs,
      y = r.y * sMs,
    )

  infix def sandwich(r: Pga2dPoint): Pga2dProjectivePoint =
    val sMs = s * s
    Pga2dProjectivePoint(
      x = (r.x * sMs - 2.0 * s * wx),
      y = (r.y * sMs - 2.0 * s * wy),
      w = sMs,
    )

  infix def sandwich(r: Pga2dLineIdeal): Pga2dLine =
    val sMs = s * s
    Pga2dLine(
      x = r.x * sMs,
      y = r.y * sMs,
      w = 2.0 * s * (r.x * wx + r.y * wy),
    )

  infix def sandwich(r: Pga2dPseudoScalar): Pga2dPseudoScalar =
    Pga2dPseudoScalar(
      i = r.i * s * s,
    )

  infix def sandwich(r: Pga2dPointCenter.type): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = -2.0 * s * wx,
      y = -2.0 * s * wy,
      w = s * s,
    )

  infix def reverseSandwich(r: Pga2dMotor): Pga2dMotor =
    val sMs = s * s
    Pga2dMotor(
      s = r.s * sMs,
      wx = (r.wx * sMs + 2.0 * r.xy * s * wy),
      wy = (r.wy * sMs - 2.0 * r.xy * s * wx),
      xy = r.xy * sMs,
    )

  infix def reverseSandwich(r: Pga2dLine): Pga2dLine =
    val sMs = s * s
    Pga2dLine(
      x = r.x * sMs,
      y = r.y * sMs,
      w = (r.w * sMs + 2.0 * s * (-r.x * wx - r.y * wy)),
    )

  infix def reverseSandwich(r: Pga2dProjectivePoint): Pga2dProjectivePoint =
    val sMs = s * s
    Pga2dProjectivePoint(
      x = (r.x * sMs + 2.0 * r.w * s * wx),
      y = (r.y * sMs + 2.0 * r.w * s * wy),
      w = r.w * sMs,
    )

  infix def reverseSandwich(r: Pga2dRotor): Pga2dMotor =
    val sMs = s * s
    Pga2dMotor(
      s = r.s * sMs,
      wx = 2.0 * r.xy * s * wy,
      wy = -2.0 * r.xy * s * wx,
      xy = r.xy * sMs,
    )

  infix def reverseSandwich(r: Pga2dProjectiveTranslator): Pga2dProjectiveTranslator =
    val sMs = s * s
    Pga2dProjectiveTranslator(
      s = r.s * sMs,
      wx = r.wx * sMs,
      wy = r.wy * sMs,
    )

  infix def reverseSandwich(r: Pga2dTranslator): Pga2dProjectiveTranslator =
    val sMs = s * s
    Pga2dProjectiveTranslator(
      s = sMs,
      wx = r.wx * sMs,
      wy = r.wy * sMs,
    )

  infix def reverseSandwich(r: Pga2dVector): Pga2dVector =
    val sMs = s * s
    Pga2dVector(
      x = r.x * sMs,
      y = r.y * sMs,
    )

  infix def reverseSandwich(r: Pga2dPoint): Pga2dProjectivePoint =
    val sMs = s * s
    Pga2dProjectivePoint(
      x = (r.x * sMs + 2.0 * s * wx),
      y = (r.y * sMs + 2.0 * s * wy),
      w = sMs,
    )

  infix def reverseSandwich(r: Pga2dLineIdeal): Pga2dLine =
    val sMs = s * s
    Pga2dLine(
      x = r.x * sMs,
      y = r.y * sMs,
      w = 2.0 * s * (-r.x * wx - r.y * wy),
    )

  infix def reverseSandwich(r: Pga2dPseudoScalar): Pga2dPseudoScalar =
    Pga2dPseudoScalar(
      i = r.i * s * s,
    )

  infix def reverseSandwich(r: Pga2dPointCenter.type): Pga2dProjectivePoint =
    Pga2dProjectivePoint(
      x = 2.0 * s * wx,
      y = 2.0 * s * wy,
      w = s * s,
    )

  infix def cross(r: Pga2dMotor): Pga2dVector =
    Pga2dVector(
      x = -r.xy * wx,
      y = -r.xy * wy,
    )

  infix def cross(r: Pga2dLine): Pga2dLine =
    Pga2dLine(
      x = 0.0,
      y = 0.0,
      w = (r.x * wx + r.y * wy),
    )

  infix def cross(r: Pga2dProjectivePoint): Pga2dVector =
    Pga2dVector(
      x = -r.w * wx,
      y = -r.w * wy,
    )

  infix def cross(r: Pga2dRotor): Pga2dVector =
    Pga2dVector(
      x = -r.xy * wx,
      y = -r.xy * wy,
    )

  infix def cross(r: Pga2dPoint): Pga2dVector =
    Pga2dVector(
      x = -wx,
      y = -wy,
    )

  infix def cross(r: Pga2dLineIdeal): Pga2dLine =
    Pga2dLine(
      x = 0.0,
      y = 0.0,
      w = (r.x * wx + r.y * wy),
    )

  infix def cross(r: Pga2dPointCenter.type): Pga2dVector =
    Pga2dVector(
      x = -wx,
      y = -wy,
    )


object Pga2dProjectiveTranslator:

  inline val componentsCount = 3

  val zero: Pga2dProjectiveTranslator = Pga2dProjectiveTranslator(0.0, 0.0, 0.0)