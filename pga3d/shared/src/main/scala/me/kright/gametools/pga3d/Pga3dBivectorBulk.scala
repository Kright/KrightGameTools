package me.kright.gametools.pga3d

import scala.annotation.targetName
import me.kright.gametools.flatarray.FlatDoubleSerializer


/**
 * The bulk part (xy, xz, yz) of a Pga3dBivector: a line passing through the center of coordinates,
 * or the angular part of a rate of motion. bivectorBulk.exp is a Pga3dRotor.
 *
 * Variable fields: xy, xz, yz.
 *
 * Generated by pgaNdCodeGen (me.kright.gametools.pga.codegen.scalagen.pga3d.runScalaCodeGen).
 * Do not edit by hand: change the generator and re-run it.
 */
final case class Pga3dBivectorBulk(xy: Double = 0.0,
                                   xz: Double = 0.0,
                                   yz: Double = 0.0) derives CanEqual, FlatDoubleSerializer:

  override def toString: String =
    s"Pga3dBivectorBulk(xy = $xy, xz = $xz, yz = $yz)"

  def dual: Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = yz,
      wy = -xz,
      wz = xy,
    )

  def bulk: Pga3dBivectorBulk =
    this

  @targetName("unaryMinus")
  def unary_- : Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = -xy,
      xz = -xz,
      yz = -yz,
    )

  def reverse: Pga3dBivectorBulk =
    -this

  def antiReverse: Pga3dBivectorBulk =
    -this

  def bulkNormSquare: Double =
    (xy * xy + xz * xz + yz * yz)

  def bulkNorm: Double =
    Math.sqrt(bulkNormSquare)

  def normalizedByBulk =
    this / bulkNorm

  def normSquare: Double =
    (xy * xy + xz * xz + yz * yz)

  def norm: Double =
    Math.sqrt(normSquare)

  def normalizedByNorm =
    this / norm

  @targetName("times")
  def *(r: Double): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = r * xy,
      xz = r * xz,
      yz = r * yz,
    )

  /** multiplies by the reciprocal: one division instead of one per component, at the cost of one extra rounding (~0.5 ulp) */
  @targetName("div")
  def /(r: Double): Pga3dBivectorBulk =
    this * (1.0 / r)

  @targetName("plus")
  def +(r: Pga3dBivector): Pga3dBivector =
    Pga3dBivector(
      wx = r.wx,
      wy = r.wy,
      wz = r.wz,
      xy = (r.xy + xy),
      xz = (r.xz + xz),
      yz = (r.yz + yz),
    )

  @targetName("minus")
  def -(r: Pga3dBivector): Pga3dBivector =
    Pga3dBivector(
      wx = -r.wx,
      wy = -r.wy,
      wz = -r.wz,
      xy = (xy - r.xy),
      xz = (xz - r.xz),
      yz = (yz - r.yz),
    )

  def madd(r: Pga3dBivector, mult: Double): Pga3dBivector =
    Pga3dBivector(
      wx = mult * r.wx,
      wy = mult * r.wy,
      wz = mult * r.wz,
      xy = (xy + mult * r.xy),
      xz = (xz + mult * r.xz),
      yz = (yz + mult * r.yz),
    )

  @targetName("plus")
  def +(r: Pga3dBivectorBulk): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = (r.xy + xy),
      xz = (r.xz + xz),
      yz = (r.yz + yz),
    )

  @targetName("minus")
  def -(r: Pga3dBivectorBulk): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = (xy - r.xy),
      xz = (xz - r.xz),
      yz = (yz - r.yz),
    )

  def madd(r: Pga3dBivectorBulk, mult: Double): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = (xy + mult * r.xy),
      xz = (xz + mult * r.xz),
      yz = (yz + mult * r.yz),
    )

  @targetName("plus")
  def +(r: Pga3dBivectorWeight): Pga3dBivector =
    Pga3dBivector(
      wx = r.wx,
      wy = r.wy,
      wz = r.wz,
      xy = xy,
      xz = xz,
      yz = yz,
    )

  @targetName("minus")
  def -(r: Pga3dBivectorWeight): Pga3dBivector =
    Pga3dBivector(
      wx = -r.wx,
      wy = -r.wy,
      wz = -r.wz,
      xy = xy,
      xz = xz,
      yz = yz,
    )

  def madd(r: Pga3dBivectorWeight, mult: Double): Pga3dBivector =
    Pga3dBivector(
      wx = mult * r.wx,
      wy = mult * r.wy,
      wz = mult * r.wz,
      xy = xy,
      xz = xz,
      yz = yz,
    )

  /** componentwise product on the raw fields; NOT a geometric-algebra operation (basis-dependent) */
  def scale(r: Pga3dBivectorBulk): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = xy * r.xy,
      xz = xz * r.xz,
      yz = yz * r.yz,
    )

  /** componentwise 1/x on the raw fields (a zero field yields Infinity); NOT the multiplicative inverse */
  def reciprocal: Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = 1.0 / xy,
      xz = 1.0 / xz,
      yz = 1.0 / yz,
    )

  def exp: Pga3dRotor =
    val len = bulkNorm
    val cos = Math.cos(len)

    // sin(x)/x = 1 - x^2/6 + x^4/120 - ...; at x <= 1e-5 the dropped x^4/120 <= 8.4e-23
    // relative term is far below 1e-17, so the second-order form is exact in double
    val sinDivLen = if (len > 1e-5) {
      Math.sin(len) / len
    } else {
      1.0 - (len * len) / 6.0
    }

    Pga3dRotor(
      s = cos,
      xy = sinDivLen * xy,
      xz = sinDivLen * xz,
      yz = sinDivLen * yz,
    )

  def exp(t: Double): Pga3dRotor =
    val len = bulkNorm * Math.abs(t)
    val cos = Math.cos(len)

    // sin(x)/x = 1 - x^2/6 + x^4/120 - ...; at x <= 1e-5 the dropped x^4/120 <= 8.4e-23
    // relative term is far below 1e-17, so the second-order form is exact in double
    val sinDivLen = if (len > 1e-5) {
      Math.sin(len) / len
    } else {
      1.0 - (len * len) / 6.0
    }

    Pga3dRotor(
      s = cos,
      xy = sinDivLen * t * xy,
      xz = sinDivLen * t * xz,
      yz = sinDivLen * t * yz,
    )

  def toMultivector: Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = 0.0,
      x = 0.0,
      y = 0.0,
      z = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = xy,
      xz = xz,
      yz = yz,
      wxy = 0.0,
      wxz = 0.0,
      wyz = 0.0,
      xyz = 0.0,
      i = 0.0,
    )

  def toMotor: Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = xy,
      xz = xz,
      yz = yz,
      i = 0.0,
    )

  def toBivector: Pga3dBivector =
    Pga3dBivector(
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = xy,
      xz = xz,
      yz = yz,
    )

  def toRotor: Pga3dRotor =
    Pga3dRotor(
      s = 0.0,
      xy = xy,
      xz = xz,
      yz = yz,
    )

  infix def geometric(r: Pga3dMotor): Pga3dMotor =
    Pga3dMotor(
      s = (-r.xy * xy - r.xz * xz - r.yz * yz),
      wx = (r.wy * xy + r.wz * xz - r.i * yz),
      wy = (r.i * xz + r.wz * yz - r.wx * xy),
      wz = (-r.i * xy - r.wx * xz - r.wy * yz),
      xy = (r.s * xy + r.xz * yz - r.yz * xz),
      xz = (r.s * xz + r.yz * xy - r.xy * yz),
      yz = (r.s * yz + r.xy * xz - r.xz * xy),
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  infix def geometric(r: Pga3dPlane): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = 0.0,
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = r.w * xy,
      wxz = r.w * xz,
      wyz = r.w * yz,
      xyz = (r.x * yz + r.z * xy - r.y * xz),
      i = 0.0,
    )

  infix def geometric(r: Pga3dBivector): Pga3dMotor =
    Pga3dMotor(
      s = (-r.xy * xy - r.xz * xz - r.yz * yz),
      wx = (r.wy * xy + r.wz * xz),
      wy = (r.wz * yz - r.wx * xy),
      wz = (-r.wx * xz - r.wy * yz),
      xy = (r.xz * yz - r.yz * xz),
      xz = (r.yz * xy - r.xy * yz),
      yz = (r.xy * xz - r.xz * xy),
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  infix def geometric(r: Pga3dProjectivePoint): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = (r.x * yz + r.z * xy - r.y * xz),
      x = -r.w * yz,
      y = r.w * xz,
      z = -r.w * xy,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = (r.x * xz + r.y * yz),
      wxz = (r.z * yz - r.x * xy),
      wyz = (-r.y * xy - r.z * xz),
      xyz = 0.0,
      i = 0.0,
    )

  infix def geometric(r: Pga3dRotor): Pga3dRotor =
    Pga3dRotor(
      s = (-r.xy * xy - r.xz * xz - r.yz * yz),
      xy = (r.s * xy + r.xz * yz - r.yz * xz),
      xz = (r.s * xz + r.yz * xy - r.xy * yz),
      yz = (r.s * yz + r.xy * xz - r.xz * xy),
    )

  infix def geometric(r: Pga3dProjectiveTranslator): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (r.wy * xy + r.wz * xz),
      wy = (r.wz * yz - r.wx * xy),
      wz = (-r.wx * xz - r.wy * yz),
      xy = r.s * xy,
      xz = r.s * xz,
      yz = r.s * yz,
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  infix def geometric(r: Pga3dTranslator): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (r.wy * xy + r.wz * xz),
      wy = (r.wz * yz - r.wx * xy),
      wz = (-r.wx * xz - r.wy * yz),
      xy = xy,
      xz = xz,
      yz = yz,
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  infix def geometric(r: Pga3dVector): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = (r.x * yz + r.z * xy - r.y * xz),
      x = 0.0,
      y = 0.0,
      z = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = (r.x * xz + r.y * yz),
      wxz = (r.z * yz - r.x * xy),
      wyz = (-r.y * xy - r.z * xz),
      xyz = 0.0,
      i = 0.0,
    )

  infix def geometric(r: Pga3dPoint): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = (r.x * yz + r.z * xy - r.y * xz),
      x = -yz,
      y = xz,
      z = -xy,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = (r.x * xz + r.y * yz),
      wxz = (r.z * yz - r.x * xy),
      wyz = (-r.y * xy - r.z * xz),
      xyz = 0.0,
      i = 0.0,
    )

  infix def geometric(r: Pga3dPlaneCentral): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = 0.0,
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = 0.0,
      wxz = 0.0,
      wyz = 0.0,
      xyz = (r.x * yz + r.z * xy - r.y * xz),
      i = 0.0,
    )

  infix def geometric(r: Pga3dBivectorBulk): Pga3dRotor =
    Pga3dRotor(
      s = (-r.xy * xy - r.xz * xz - r.yz * yz),
      xy = (r.xz * yz - r.yz * xz),
      xz = (r.yz * xy - r.xy * yz),
      yz = (r.xy * xz - r.xz * xy),
    )

  infix def geometric(r: Pga3dBivectorWeight): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (r.wy * xy + r.wz * xz),
      wy = (r.wz * yz - r.wx * xy),
      wz = (-r.wx * xz - r.wy * yz),
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  infix def geometric(r: Pga3dPseudoScalar): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = -r.i * yz,
      wy = r.i * xz,
      wz = -r.i * xy,
    )

  infix def geometric(r: Pga3dPointCenter.type): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = -yz,
      y = xz,
      z = -xy,
    )

  infix def dot(r: Pga3dMotor): Pga3dMotor =
    Pga3dMotor(
      s = (-r.xy * xy - r.xz * xz - r.yz * yz),
      wx = -r.i * yz,
      wy = r.i * xz,
      wz = -r.i * xy,
      xy = r.s * xy,
      xz = r.s * xz,
      yz = r.s * yz,
      i = 0.0,
    )

  infix def dot(r: Pga3dPlane): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  infix def dot(r: Pga3dBivector): Double =
    (-r.xy * xy - r.xz * xz - r.yz * yz)

  infix def dot(r: Pga3dProjectivePoint): Pga3dPlane =
    Pga3dPlane(
      x = -r.w * yz,
      y = r.w * xz,
      z = -r.w * xy,
      w = (r.x * yz + r.z * xy - r.y * xz),
    )

  infix def dot(r: Pga3dRotor): Pga3dRotor =
    Pga3dRotor(
      s = (-r.xy * xy - r.xz * xz - r.yz * yz),
      xy = r.s * xy,
      xz = r.s * xz,
      yz = r.s * yz,
    )

  infix def dot(r: Pga3dProjectiveTranslator): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = r.s * xy,
      xz = r.s * xz,
      yz = r.s * yz,
    )

  infix def dot(r: Pga3dTranslator): Pga3dBivectorBulk =
    this

  infix def dot(r: Pga3dVector): Pga3dPlane =
    Pga3dPlane(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * yz + r.z * xy - r.y * xz),
    )

  infix def dot(r: Pga3dPoint): Pga3dPlane =
    Pga3dPlane(
      x = -yz,
      y = xz,
      z = -xy,
      w = (r.x * yz + r.z * xy - r.y * xz),
    )

  infix def dot(r: Pga3dPlaneCentral): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  infix def dot(r: Pga3dBivectorBulk): Double =
    (-r.xy * xy - r.xz * xz - r.yz * yz)

  infix def dot(r: Pga3dPseudoScalar): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = -r.i * yz,
      wy = r.i * xz,
      wz = -r.i * xy,
    )

  infix def dot(r: Pga3dPointCenter.type): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = -yz,
      y = xz,
      z = -xy,
    )

  infix def wedge(r: Pga3dMotor): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = r.s * xy,
      xz = r.s * xz,
      yz = r.s * yz,
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  inline infix def ^(r: Pga3dMotor): Pga3dMotor = wedge(r)

  inline infix def meet(r: Pga3dMotor): Pga3dMotor = wedge(r)

  infix def wedge(r: Pga3dPlane): Pga3dProjectivePoint =
    Pga3dProjectivePoint(
      x = -r.w * yz,
      y = r.w * xz,
      z = -r.w * xy,
      w = (r.x * yz + r.z * xy - r.y * xz),
    )

  inline infix def ^(r: Pga3dPlane): Pga3dProjectivePoint = wedge(r)

  inline infix def meet(r: Pga3dPlane): Pga3dProjectivePoint = wedge(r)

  infix def wedge(r: Pga3dBivector): Pga3dPseudoScalar =
    Pga3dPseudoScalar(
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  inline infix def ^(r: Pga3dBivector): Pga3dPseudoScalar = wedge(r)

  inline infix def meet(r: Pga3dBivector): Pga3dPseudoScalar = wedge(r)

  infix def wedge(r: Pga3dRotor): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = r.s * xy,
      xz = r.s * xz,
      yz = r.s * yz,
    )

  inline infix def ^(r: Pga3dRotor): Pga3dBivectorBulk = wedge(r)

  inline infix def meet(r: Pga3dRotor): Pga3dBivectorBulk = wedge(r)

  infix def wedge(r: Pga3dProjectiveTranslator): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = r.s * xy,
      xz = r.s * xz,
      yz = r.s * yz,
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  inline infix def ^(r: Pga3dProjectiveTranslator): Pga3dMotor = wedge(r)

  inline infix def meet(r: Pga3dProjectiveTranslator): Pga3dMotor = wedge(r)

  infix def wedge(r: Pga3dTranslator): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = xy,
      xz = xz,
      yz = yz,
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  inline infix def ^(r: Pga3dTranslator): Pga3dMotor = wedge(r)

  inline infix def meet(r: Pga3dTranslator): Pga3dMotor = wedge(r)

  infix def wedge(r: Pga3dPlaneCentral): Pga3dProjectivePoint =
    Pga3dProjectivePoint(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * yz + r.z * xy - r.y * xz),
    )

  inline infix def ^(r: Pga3dPlaneCentral): Pga3dProjectivePoint = wedge(r)

  inline infix def meet(r: Pga3dPlaneCentral): Pga3dProjectivePoint = wedge(r)

  infix def wedge(r: Pga3dBivectorWeight): Pga3dPseudoScalar =
    Pga3dPseudoScalar(
      i = (r.wx * yz + r.wz * xy - r.wy * xz),
    )

  inline infix def ^(r: Pga3dBivectorWeight): Pga3dPseudoScalar = wedge(r)

  inline infix def meet(r: Pga3dBivectorWeight): Pga3dPseudoScalar = wedge(r)

  infix def antiGeometric(r: Pga3dMotor): Pga3dRotor =
    Pga3dRotor(
      s = (r.wx * yz + r.wz * xy - r.wy * xz),
      xy = (r.i * xy - r.wx * xz - r.wy * yz),
      xz = (r.i * xz + r.wx * xy - r.wz * yz),
      yz = (r.i * yz + r.wy * xy + r.wz * xz),
    )

  infix def antiGeometric(r: Pga3dPlane): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = -r.w * yz,
      y = r.w * xz,
      z = -r.w * xy,
    )

  infix def antiGeometric(r: Pga3dBivector): Pga3dRotor =
    Pga3dRotor(
      s = (r.wx * yz + r.wz * xy - r.wy * xz),
      xy = (-r.wx * xz - r.wy * yz),
      xz = (r.wx * xy - r.wz * yz),
      yz = (r.wy * xy + r.wz * xz),
    )

  infix def antiGeometric(r: Pga3dProjectivePoint): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = 0.0,
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = 0.0,
      wxz = 0.0,
      wyz = 0.0,
      xyz = (r.x * yz + r.z * xy - r.y * xz),
      i = 0.0,
    )

  infix def antiGeometric(r: Pga3dProjectiveTranslator): Pga3dRotor =
    Pga3dRotor(
      s = (r.wx * yz + r.wz * xy - r.wy * xz),
      xy = (-r.wx * xz - r.wy * yz),
      xz = (r.wx * xy - r.wz * yz),
      yz = (r.wy * xy + r.wz * xz),
    )

  infix def antiGeometric(r: Pga3dTranslator): Pga3dRotor =
    Pga3dRotor(
      s = (r.wx * yz + r.wz * xy - r.wy * xz),
      xy = (-r.wx * xz - r.wy * yz),
      xz = (r.wx * xy - r.wz * yz),
      yz = (r.wy * xy + r.wz * xz),
    )

  infix def antiGeometric(r: Pga3dVector): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = 0.0,
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = 0.0,
      wxz = 0.0,
      wyz = 0.0,
      xyz = (r.x * yz + r.z * xy - r.y * xz),
      i = 0.0,
    )

  infix def antiGeometric(r: Pga3dPoint): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = 0.0,
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = 0.0,
      wxz = 0.0,
      wyz = 0.0,
      xyz = (r.x * yz + r.z * xy - r.y * xz),
      i = 0.0,
    )

  infix def antiGeometric(r: Pga3dBivectorWeight): Pga3dRotor =
    Pga3dRotor(
      s = (r.wx * yz + r.wz * xy - r.wy * xz),
      xy = (-r.wx * xz - r.wy * yz),
      xz = (r.wx * xy - r.wz * yz),
      yz = (r.wy * xy + r.wz * xz),
    )

  infix def antiGeometric(r: Pga3dPseudoScalar): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = r.i * xy,
      xz = r.i * xz,
      yz = r.i * yz,
    )

  infix def antiDot(r: Pga3dMotor): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = r.i * xy,
      xz = r.i * xz,
      yz = r.i * yz,
    )

  infix def antiDot(r: Pga3dProjectivePoint): Pga3dProjectivePoint =
    Pga3dProjectivePoint(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * yz + r.z * xy - r.y * xz),
    )

  infix def antiDot(r: Pga3dVector): Pga3dProjectivePoint =
    Pga3dProjectivePoint(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * yz + r.z * xy - r.y * xz),
    )

  infix def antiDot(r: Pga3dPoint): Pga3dProjectivePoint =
    Pga3dProjectivePoint(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * yz + r.z * xy - r.y * xz),
    )

  infix def antiDot(r: Pga3dPseudoScalar): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = r.i * xy,
      xz = r.i * xz,
      yz = r.i * yz,
    )

  infix def antiWedge(r: Pga3dMotor): Pga3dRotor =
    Pga3dRotor(
      s = (r.wx * yz + r.wz * xy - r.wy * xz),
      xy = r.i * xy,
      xz = r.i * xz,
      yz = r.i * yz,
    )

  inline infix def v(r: Pga3dMotor): Pga3dRotor = antiWedge(r)

  inline infix def join(r: Pga3dMotor): Pga3dRotor = antiWedge(r)

  infix def antiWedge(r: Pga3dBivector): Double =
    (r.wx * yz + r.wz * xy - r.wy * xz)

  inline infix def v(r: Pga3dBivector): Double = antiWedge(r)

  inline infix def join(r: Pga3dBivector): Double = antiWedge(r)

  infix def antiWedge(r: Pga3dProjectivePoint): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  inline infix def v(r: Pga3dProjectivePoint): Pga3dPlaneCentral = antiWedge(r)

  inline infix def join(r: Pga3dProjectivePoint): Pga3dPlaneCentral = antiWedge(r)

  infix def antiWedge(r: Pga3dProjectiveTranslator): Double =
    (r.wx * yz + r.wz * xy - r.wy * xz)

  inline infix def v(r: Pga3dProjectiveTranslator): Double = antiWedge(r)

  inline infix def join(r: Pga3dProjectiveTranslator): Double = antiWedge(r)

  infix def antiWedge(r: Pga3dTranslator): Double =
    (r.wx * yz + r.wz * xy - r.wy * xz)

  inline infix def v(r: Pga3dTranslator): Double = antiWedge(r)

  inline infix def join(r: Pga3dTranslator): Double = antiWedge(r)

  infix def antiWedge(r: Pga3dVector): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  inline infix def v(r: Pga3dVector): Pga3dPlaneCentral = antiWedge(r)

  inline infix def join(r: Pga3dVector): Pga3dPlaneCentral = antiWedge(r)

  infix def antiWedge(r: Pga3dPoint): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  inline infix def v(r: Pga3dPoint): Pga3dPlaneCentral = antiWedge(r)

  inline infix def join(r: Pga3dPoint): Pga3dPlaneCentral = antiWedge(r)

  infix def antiWedge(r: Pga3dBivectorWeight): Double =
    (r.wx * yz + r.wz * xy - r.wy * xz)

  inline infix def v(r: Pga3dBivectorWeight): Double = antiWedge(r)

  inline infix def join(r: Pga3dBivectorWeight): Double = antiWedge(r)

  infix def antiWedge(r: Pga3dPseudoScalar): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = r.i * xy,
      xz = r.i * xz,
      yz = r.i * yz,
    )

  inline infix def v(r: Pga3dPseudoScalar): Pga3dBivectorBulk = antiWedge(r)

  inline infix def join(r: Pga3dPseudoScalar): Pga3dBivectorBulk = antiWedge(r)

  infix def sandwich(r: Pga3dMotor): Pga3dMotor =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dMotor(
      s = r.s * (xyMxy + xzMxz + yzMyz),
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
      xy = (2.0 * (r.xz * xyMxz + r.yz * xyMyz) + r.xy * (xyMxy - xzMxz - yzMyz)),
      xz = (2.0 * (r.xy * xyMxz + r.yz * xzMyz) + r.xz * (xzMxz - xyMxy - yzMyz)),
      yz = (2.0 * (r.xy * xyMyz + r.xz * xzMyz) + r.yz * (yzMyz - xyMxy - xzMxz)),
      i = r.i * (xyMxy + xzMxz + yzMyz),
    )

  infix def sandwich(r: Pga3dPlane): Pga3dPlane =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dPlane(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
      w = r.w * (xyMxy + xzMxz + yzMyz),
    )

  infix def sandwich(r: Pga3dBivector): Pga3dBivector =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dBivector(
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
      xy = (2.0 * (r.xz * xyMxz + r.yz * xyMyz) + r.xy * (xyMxy - xzMxz - yzMyz)),
      xz = (2.0 * (r.xy * xyMxz + r.yz * xzMyz) + r.xz * (xzMxz - xyMxy - yzMyz)),
      yz = (2.0 * (r.xy * xyMyz + r.xz * xzMyz) + r.yz * (yzMyz - xyMxy - xzMxz)),
    )

  infix def sandwich(r: Pga3dProjectivePoint): Pga3dProjectivePoint =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dProjectivePoint(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
      w = r.w * (xyMxy + xzMxz + yzMyz),
    )

  infix def sandwich(r: Pga3dRotor): Pga3dRotor =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dRotor(
      s = r.s * (xyMxy + xzMxz + yzMyz),
      xy = (2.0 * (r.xz * xyMxz + r.yz * xyMyz) + r.xy * (xyMxy - xzMxz - yzMyz)),
      xz = (2.0 * (r.xy * xyMxz + r.yz * xzMyz) + r.xz * (xzMxz - xyMxy - yzMyz)),
      yz = (2.0 * (r.xy * xyMyz + r.xz * xzMyz) + r.yz * (yzMyz - xyMxy - xzMxz)),
    )

  infix def sandwich(r: Pga3dProjectiveTranslator): Pga3dProjectiveTranslator =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dProjectiveTranslator(
      s = r.s * (xyMxy + xzMxz + yzMyz),
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
    )

  infix def sandwich(r: Pga3dTranslator): Pga3dProjectiveTranslator =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dProjectiveTranslator(
      s = (xyMxy + xzMxz + yzMyz),
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
    )

  infix def sandwich(r: Pga3dVector): Pga3dVector =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dVector(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
    )

  infix def sandwich(r: Pga3dPoint): Pga3dProjectivePoint =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dProjectivePoint(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
      w = (xyMxy + xzMxz + yzMyz),
    )

  infix def sandwich(r: Pga3dPlaneCentral): Pga3dPlaneCentral =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dPlaneCentral(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
    )

  infix def sandwich(r: Pga3dBivectorBulk): Pga3dBivectorBulk =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dBivectorBulk(
      xy = (2.0 * (r.xz * xyMxz + r.yz * xyMyz) + r.xy * (xyMxy - xzMxz - yzMyz)),
      xz = (2.0 * (r.xy * xyMxz + r.yz * xzMyz) + r.xz * (xzMxz - xyMxy - yzMyz)),
      yz = (2.0 * (r.xy * xyMyz + r.xz * xzMyz) + r.yz * (yzMyz - xyMxy - xzMxz)),
    )

  infix def sandwich(r: Pga3dBivectorWeight): Pga3dBivectorWeight =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dBivectorWeight(
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
    )

  infix def sandwich(r: Pga3dPseudoScalar): Pga3dPseudoScalar =
    Pga3dPseudoScalar(
      i = r.i * (xy * xy + xz * xz + yz * yz),
    )

  infix def sandwich(r: Pga3dPointCenter.type): Pga3dProjectivePoint =
    Pga3dProjectivePoint(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (xy * xy + xz * xz + yz * yz),
    )

  infix def reverseSandwich(r: Pga3dMotor): Pga3dMotor =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dMotor(
      s = r.s * (xyMxy + xzMxz + yzMyz),
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
      xy = (2.0 * (r.xz * xyMxz + r.yz * xyMyz) + r.xy * (xyMxy - xzMxz - yzMyz)),
      xz = (2.0 * (r.xy * xyMxz + r.yz * xzMyz) + r.xz * (xzMxz - xyMxy - yzMyz)),
      yz = (2.0 * (r.xy * xyMyz + r.xz * xzMyz) + r.yz * (yzMyz - xyMxy - xzMxz)),
      i = r.i * (xyMxy + xzMxz + yzMyz),
    )

  infix def reverseSandwich(r: Pga3dPlane): Pga3dPlane =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dPlane(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
      w = r.w * (xyMxy + xzMxz + yzMyz),
    )

  infix def reverseSandwich(r: Pga3dBivector): Pga3dBivector =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dBivector(
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
      xy = (2.0 * (r.xz * xyMxz + r.yz * xyMyz) + r.xy * (xyMxy - xzMxz - yzMyz)),
      xz = (2.0 * (r.xy * xyMxz + r.yz * xzMyz) + r.xz * (xzMxz - xyMxy - yzMyz)),
      yz = (2.0 * (r.xy * xyMyz + r.xz * xzMyz) + r.yz * (yzMyz - xyMxy - xzMxz)),
    )

  infix def reverseSandwich(r: Pga3dProjectivePoint): Pga3dProjectivePoint =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dProjectivePoint(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
      w = r.w * (xyMxy + xzMxz + yzMyz),
    )

  infix def reverseSandwich(r: Pga3dRotor): Pga3dRotor =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dRotor(
      s = r.s * (xyMxy + xzMxz + yzMyz),
      xy = (2.0 * (r.xz * xyMxz + r.yz * xyMyz) + r.xy * (xyMxy - xzMxz - yzMyz)),
      xz = (2.0 * (r.xy * xyMxz + r.yz * xzMyz) + r.xz * (xzMxz - xyMxy - yzMyz)),
      yz = (2.0 * (r.xy * xyMyz + r.xz * xzMyz) + r.yz * (yzMyz - xyMxy - xzMxz)),
    )

  infix def reverseSandwich(r: Pga3dProjectiveTranslator): Pga3dProjectiveTranslator =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dProjectiveTranslator(
      s = r.s * (xyMxy + xzMxz + yzMyz),
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
    )

  infix def reverseSandwich(r: Pga3dTranslator): Pga3dProjectiveTranslator =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dProjectiveTranslator(
      s = (xyMxy + xzMxz + yzMyz),
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
    )

  infix def reverseSandwich(r: Pga3dVector): Pga3dVector =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dVector(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
    )

  infix def reverseSandwich(r: Pga3dPoint): Pga3dProjectivePoint =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dProjectivePoint(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
      w = (xyMxy + xzMxz + yzMyz),
    )

  infix def reverseSandwich(r: Pga3dPlaneCentral): Pga3dPlaneCentral =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dPlaneCentral(
      x = (2.0 * (r.z * xyMyz - r.y * xzMyz) + r.x * (yzMyz - xyMxy - xzMxz)),
      y = (2.0 * (-r.x * xzMyz - r.z * xyMxz) + r.y * (xzMxz - xyMxy - yzMyz)),
      z = (2.0 * (r.x * xyMyz - r.y * xyMxz) + r.z * (xyMxy - xzMxz - yzMyz)),
    )

  infix def reverseSandwich(r: Pga3dBivectorBulk): Pga3dBivectorBulk =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dBivectorBulk(
      xy = (2.0 * (r.xz * xyMxz + r.yz * xyMyz) + r.xy * (xyMxy - xzMxz - yzMyz)),
      xz = (2.0 * (r.xy * xyMxz + r.yz * xzMyz) + r.xz * (xzMxz - xyMxy - yzMyz)),
      yz = (2.0 * (r.xy * xyMyz + r.xz * xzMyz) + r.yz * (yzMyz - xyMxy - xzMxz)),
    )

  infix def reverseSandwich(r: Pga3dBivectorWeight): Pga3dBivectorWeight =
    val xyMxy = xy * xy
    val xyMxz = xy * xz
    val xyMyz = xy * yz
    val xzMxz = xz * xz
    val xzMyz = xz * yz
    val yzMyz = yz * yz
    Pga3dBivectorWeight(
      wx = (2.0 * (r.wz * xyMyz - r.wy * xzMyz) + r.wx * (yzMyz - xyMxy - xzMxz)),
      wy = (2.0 * (-r.wx * xzMyz - r.wz * xyMxz) + r.wy * (xzMxz - xyMxy - yzMyz)),
      wz = (2.0 * (r.wx * xyMyz - r.wy * xyMxz) + r.wz * (xyMxy - xzMxz - yzMyz)),
    )

  infix def reverseSandwich(r: Pga3dPseudoScalar): Pga3dPseudoScalar =
    Pga3dPseudoScalar(
      i = r.i * (xy * xy + xz * xz + yz * yz),
    )

  infix def reverseSandwich(r: Pga3dPointCenter.type): Pga3dProjectivePoint =
    Pga3dProjectivePoint(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (xy * xy + xz * xz + yz * yz),
    )

  infix def cross(r: Pga3dMotor): Pga3dBivector =
    Pga3dBivector(
      wx = (r.wy * xy + r.wz * xz),
      wy = (r.wz * yz - r.wx * xy),
      wz = (-r.wx * xz - r.wy * yz),
      xy = (r.xz * yz - r.yz * xz),
      xz = (r.yz * xy - r.xy * yz),
      yz = (r.xy * xz - r.xz * xy),
    )

  infix def cross(r: Pga3dPlane): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  infix def cross(r: Pga3dBivector): Pga3dBivector =
    Pga3dBivector(
      wx = (r.wy * xy + r.wz * xz),
      wy = (r.wz * yz - r.wx * xy),
      wz = (-r.wx * xz - r.wy * yz),
      xy = (r.xz * yz - r.yz * xz),
      xz = (r.yz * xy - r.xy * yz),
      yz = (r.xy * xz - r.xz * xy),
    )

  infix def cross(r: Pga3dProjectivePoint): Pga3dVector =
    Pga3dVector(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  infix def cross(r: Pga3dRotor): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = (r.xz * yz - r.yz * xz),
      xz = (r.yz * xy - r.xy * yz),
      yz = (r.xy * xz - r.xz * xy),
    )

  infix def cross(r: Pga3dProjectiveTranslator): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (r.wy * xy + r.wz * xz),
      wy = (r.wz * yz - r.wx * xy),
      wz = (-r.wx * xz - r.wy * yz),
    )

  infix def cross(r: Pga3dTranslator): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (r.wy * xy + r.wz * xz),
      wy = (r.wz * yz - r.wx * xy),
      wz = (-r.wx * xz - r.wy * yz),
    )

  infix def cross(r: Pga3dVector): Pga3dVector =
    Pga3dVector(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  infix def cross(r: Pga3dPoint): Pga3dVector =
    Pga3dVector(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  infix def cross(r: Pga3dPlaneCentral): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = (r.y * xy + r.z * xz),
      y = (r.z * yz - r.x * xy),
      z = (-r.x * xz - r.y * yz),
    )

  infix def cross(r: Pga3dBivectorBulk): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = (r.xz * yz - r.yz * xz),
      xz = (r.yz * xy - r.xy * yz),
      yz = (r.xy * xz - r.xz * xy),
    )

  infix def cross(r: Pga3dBivectorWeight): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (r.wy * xy + r.wz * xz),
      wy = (r.wz * yz - r.wx * xy),
      wz = (-r.wx * xz - r.wy * yz),
    )


object Pga3dBivectorBulk:

  inline val componentsCount = 3

  val zero: Pga3dBivectorBulk = Pga3dBivectorBulk(0.0, 0.0, 0.0)