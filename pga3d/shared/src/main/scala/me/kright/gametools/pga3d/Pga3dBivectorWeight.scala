package me.kright.gametools.pga3d

import scala.annotation.targetName
import me.kright.gametools.flatarray.FlatDoubleSerializer


/**
 * The weight part (wx, wy, wz) of a Pga3dBivector: an ideal line, or the linear part of a rate of motion.
 * bivectorWeight.exp is a Pga3dTranslator.
 *
 * Variable fields: wx, wy, wz.
 *
 * Generated by pgaNdCodeGen (me.kright.gametools.pga.codegen.scalagen.pga3d.runScalaCodeGen).
 * Do not edit by hand: change the generator and re-run it.
 */
final case class Pga3dBivectorWeight(wx: Double = 0.0,
                                     wy: Double = 0.0,
                                     wz: Double = 0.0) derives CanEqual, FlatDoubleSerializer:

  override def toString: String =
    s"Pga3dBivectorWeight(wx = $wx, wy = $wy, wz = $wz)"

  def dual: Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = wz,
      xz = -wy,
      yz = wx,
    )

  def weight: Pga3dBivectorWeight =
    this

  @targetName("unaryMinus")
  def unary_- : Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = -wx,
      wy = -wy,
      wz = -wz,
    )

  def reverse: Pga3dBivectorWeight =
    -this

  def antiReverse: Pga3dBivectorWeight =
    -this

  def weightNormSquare: Double =
    (wx * wx + wy * wy + wz * wz)

  def weightNorm: Double =
    Math.sqrt(weightNormSquare)

  def normalizedByWeight =
    this / weightNorm

  def normSquare: Double =
    (wx * wx + wy * wy + wz * wz)

  def norm: Double =
    Math.sqrt(normSquare)

  def normalizedByNorm =
    this / norm

  @targetName("times")
  def *(r: Double): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = r * wx,
      wy = r * wy,
      wz = r * wz,
    )

  /** multiplies by the reciprocal: one division instead of one per component, at the cost of one extra rounding (~0.5 ulp) */
  @targetName("div")
  def /(r: Double): Pga3dBivectorWeight =
    this * (1.0 / r)

  @targetName("plus")
  def +(r: Pga3dBivector): Pga3dBivector =
    Pga3dBivector(
      wx = (r.wx + wx),
      wy = (r.wy + wy),
      wz = (r.wz + wz),
      xy = r.xy,
      xz = r.xz,
      yz = r.yz,
    )

  @targetName("minus")
  def -(r: Pga3dBivector): Pga3dBivector =
    Pga3dBivector(
      wx = (wx - r.wx),
      wy = (wy - r.wy),
      wz = (wz - r.wz),
      xy = -r.xy,
      xz = -r.xz,
      yz = -r.yz,
    )

  def madd(r: Pga3dBivector, mult: Double): Pga3dBivector =
    Pga3dBivector(
      wx = (wx + mult * r.wx),
      wy = (wy + mult * r.wy),
      wz = (wz + mult * r.wz),
      xy = mult * r.xy,
      xz = mult * r.xz,
      yz = mult * r.yz,
    )

  @targetName("plus")
  def +(r: Pga3dBivectorBulk): Pga3dBivector =
    Pga3dBivector(
      wx = wx,
      wy = wy,
      wz = wz,
      xy = r.xy,
      xz = r.xz,
      yz = r.yz,
    )

  @targetName("minus")
  def -(r: Pga3dBivectorBulk): Pga3dBivector =
    Pga3dBivector(
      wx = wx,
      wy = wy,
      wz = wz,
      xy = -r.xy,
      xz = -r.xz,
      yz = -r.yz,
    )

  def madd(r: Pga3dBivectorBulk, mult: Double): Pga3dBivector =
    Pga3dBivector(
      wx = wx,
      wy = wy,
      wz = wz,
      xy = mult * r.xy,
      xz = mult * r.xz,
      yz = mult * r.yz,
    )

  @targetName("plus")
  def +(r: Pga3dBivectorWeight): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (r.wx + wx),
      wy = (r.wy + wy),
      wz = (r.wz + wz),
    )

  @targetName("minus")
  def -(r: Pga3dBivectorWeight): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (wx - r.wx),
      wy = (wy - r.wy),
      wz = (wz - r.wz),
    )

  def madd(r: Pga3dBivectorWeight, mult: Double): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (wx + mult * r.wx),
      wy = (wy + mult * r.wy),
      wz = (wz + mult * r.wz),
    )

  /** componentwise product on the raw fields; NOT a geometric-algebra operation (basis-dependent) */
  def scale(r: Pga3dBivectorWeight): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = wx * r.wx,
      wy = wy * r.wy,
      wz = wz * r.wz,
    )

  /** componentwise 1/x on the raw fields (a zero field yields Infinity); NOT the multiplicative inverse */
  def reciprocal: Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = 1.0 / wx,
      wy = 1.0 / wy,
      wz = 1.0 / wz,
    )

  def exp: Pga3dTranslator =
    Pga3dTranslator(
      wx = wx,
      wy = wy,
      wz = wz,
    )

  def exp(t: Double): Pga3dTranslator =
    Pga3dTranslator(
      wx = t * wx,
      wy = t * wy,
      wz = t * wz,
    )

  def toMultivector: Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = 0.0,
      x = 0.0,
      y = 0.0,
      z = 0.0,
      wx = wx,
      wy = wy,
      wz = wz,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = 0.0,
      wxz = 0.0,
      wyz = 0.0,
      xyz = 0.0,
      i = 0.0,
    )

  def toMotor: Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = wx,
      wy = wy,
      wz = wz,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      i = 0.0,
    )

  def toBivector: Pga3dBivector =
    Pga3dBivector(
      wx = wx,
      wy = wy,
      wz = wz,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
    )

  def toProjectiveTranslator: Pga3dProjectiveTranslator =
    Pga3dProjectiveTranslator(
      s = 0.0,
      wx = wx,
      wy = wy,
      wz = wz,
    )

  def toTranslatorUnsafe: Pga3dTranslator =
    Pga3dTranslator(
      wx = wx,
      wy = wy,
      wz = wz,
    )

  infix def geometric(r: Pga3dMotor): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (r.s * wx - r.xy * wy - r.xz * wz),
      wy = (r.s * wy + r.xy * wx - r.yz * wz),
      wz = (r.s * wz + r.xz * wx + r.yz * wy),
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      i = (r.xy * wz + r.yz * wx - r.xz * wy),
    )

  infix def geometric(r: Pga3dPlane): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
      x = 0.0,
      y = 0.0,
      z = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = (r.y * wx - r.x * wy),
      wxz = (r.z * wx - r.x * wz),
      wyz = (r.z * wy - r.y * wz),
      xyz = 0.0,
      i = 0.0,
    )

  infix def geometric(r: Pga3dBivector): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (-r.xy * wy - r.xz * wz),
      wy = (r.xy * wx - r.yz * wz),
      wz = (r.xz * wx + r.yz * wy),
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      i = (r.xy * wz + r.yz * wx - r.xz * wy),
    )

  infix def geometric(r: Pga3dProjectivePoint): Pga3dVector =
    Pga3dVector(
      x = -r.w * wx,
      y = -r.w * wy,
      z = -r.w * wz,
    )

  infix def geometric(r: Pga3dRotor): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (r.s * wx - r.xy * wy - r.xz * wz),
      wy = (r.s * wy + r.xy * wx - r.yz * wz),
      wz = (r.s * wz + r.xz * wx + r.yz * wy),
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      i = (r.xy * wz + r.yz * wx - r.xz * wy),
    )

  infix def geometric(r: Pga3dProjectiveTranslator): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = r.s * wx,
      wy = r.s * wy,
      wz = r.s * wz,
    )

  infix def geometric(r: Pga3dTranslator): Pga3dBivectorWeight =
    this

  infix def geometric(r: Pga3dPoint): Pga3dVector =
    Pga3dVector(
      x = -wx,
      y = -wy,
      z = -wz,
    )

  infix def geometric(r: Pga3dPlaneCentral): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
      x = 0.0,
      y = 0.0,
      z = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = (r.y * wx - r.x * wy),
      wxz = (r.z * wx - r.x * wz),
      wyz = (r.z * wy - r.y * wz),
      xyz = 0.0,
      i = 0.0,
    )

  infix def geometric(r: Pga3dBivectorBulk): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (-r.xy * wy - r.xz * wz),
      wy = (r.xy * wx - r.yz * wz),
      wz = (r.xz * wx + r.yz * wy),
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      i = (r.xy * wz + r.yz * wx - r.xz * wy),
    )

  infix def geometric(r: Pga3dPointCenter.type): Pga3dVector =
    Pga3dVector(
      x = -wx,
      y = -wy,
      z = -wz,
    )

  infix def dot(r: Pga3dMotor): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = r.s * wx,
      wy = r.s * wy,
      wz = r.s * wz,
    )

  infix def dot(r: Pga3dPlane): Pga3dPlane =
    Pga3dPlane(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
    )

  infix def dot(r: Pga3dRotor): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = r.s * wx,
      wy = r.s * wy,
      wz = r.s * wz,
    )

  infix def dot(r: Pga3dProjectiveTranslator): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = r.s * wx,
      wy = r.s * wy,
      wz = r.s * wz,
    )

  infix def dot(r: Pga3dTranslator): Pga3dBivectorWeight =
    this

  infix def dot(r: Pga3dPlaneCentral): Pga3dPlane =
    Pga3dPlane(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
    )

  infix def wedge(r: Pga3dMotor): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = r.s * wx,
      wy = r.s * wy,
      wz = r.s * wz,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      i = (r.xy * wz + r.yz * wx - r.xz * wy),
    )

  inline infix def ^(r: Pga3dMotor): Pga3dMotor = wedge(r)

  inline infix def meet(r: Pga3dMotor): Pga3dMotor = wedge(r)

  infix def wedge(r: Pga3dPlane): Pga3dVector =
    Pga3dVector(
      x = (r.y * wz - r.z * wy),
      y = (r.z * wx - r.x * wz),
      z = (r.x * wy - r.y * wx),
    )

  inline infix def ^(r: Pga3dPlane): Pga3dVector = wedge(r)

  inline infix def meet(r: Pga3dPlane): Pga3dVector = wedge(r)

  infix def wedge(r: Pga3dBivector): Pga3dPseudoScalar =
    Pga3dPseudoScalar(
      i = (r.xy * wz + r.yz * wx - r.xz * wy),
    )

  inline infix def ^(r: Pga3dBivector): Pga3dPseudoScalar = wedge(r)

  inline infix def meet(r: Pga3dBivector): Pga3dPseudoScalar = wedge(r)

  infix def wedge(r: Pga3dRotor): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = r.s * wx,
      wy = r.s * wy,
      wz = r.s * wz,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      i = (r.xy * wz + r.yz * wx - r.xz * wy),
    )

  inline infix def ^(r: Pga3dRotor): Pga3dMotor = wedge(r)

  inline infix def meet(r: Pga3dRotor): Pga3dMotor = wedge(r)

  infix def wedge(r: Pga3dProjectiveTranslator): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = r.s * wx,
      wy = r.s * wy,
      wz = r.s * wz,
    )

  inline infix def ^(r: Pga3dProjectiveTranslator): Pga3dBivectorWeight = wedge(r)

  inline infix def meet(r: Pga3dProjectiveTranslator): Pga3dBivectorWeight = wedge(r)

  infix def wedge(r: Pga3dTranslator): Pga3dBivectorWeight =
    this

  inline infix def ^(r: Pga3dTranslator): Pga3dBivectorWeight = wedge(r)

  inline infix def meet(r: Pga3dTranslator): Pga3dBivectorWeight = wedge(r)

  infix def wedge(r: Pga3dPlaneCentral): Pga3dVector =
    Pga3dVector(
      x = (r.y * wz - r.z * wy),
      y = (r.z * wx - r.x * wz),
      z = (r.x * wy - r.y * wx),
    )

  inline infix def ^(r: Pga3dPlaneCentral): Pga3dVector = wedge(r)

  inline infix def meet(r: Pga3dPlaneCentral): Pga3dVector = wedge(r)

  infix def wedge(r: Pga3dBivectorBulk): Pga3dPseudoScalar =
    Pga3dPseudoScalar(
      i = (r.xy * wz + r.yz * wx - r.xz * wy),
    )

  inline infix def ^(r: Pga3dBivectorBulk): Pga3dPseudoScalar = wedge(r)

  inline infix def meet(r: Pga3dBivectorBulk): Pga3dPseudoScalar = wedge(r)

  infix def antiGeometric(r: Pga3dMotor): Pga3dMotor =
    Pga3dMotor(
      s = (r.xy * wz + r.yz * wx - r.xz * wy),
      wx = (r.i * wx + r.wy * wz - r.wz * wy),
      wy = (r.i * wy + r.wz * wx - r.wx * wz),
      wz = (r.i * wz + r.wx * wy - r.wy * wx),
      xy = (r.xz * wx + r.yz * wy - r.s * wz),
      xz = (r.s * wy + r.yz * wz - r.xy * wx),
      yz = (-r.s * wx - r.xy * wy - r.xz * wz),
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiGeometric(r: Pga3dPlane): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = 0.0,
      x = (r.y * wz - r.z * wy),
      y = (r.z * wx - r.x * wz),
      z = (r.x * wy - r.y * wx),
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = r.w * wz,
      wxz = -r.w * wy,
      wyz = r.w * wx,
      xyz = (r.x * wx + r.y * wy + r.z * wz),
      i = 0.0,
    )

  infix def antiGeometric(r: Pga3dBivector): Pga3dMotor =
    Pga3dMotor(
      s = (r.xy * wz + r.yz * wx - r.xz * wy),
      wx = (r.wy * wz - r.wz * wy),
      wy = (r.wz * wx - r.wx * wz),
      wz = (r.wx * wy - r.wy * wx),
      xy = (r.xz * wx + r.yz * wy),
      xz = (r.yz * wz - r.xy * wx),
      yz = (-r.xy * wy - r.xz * wz),
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiGeometric(r: Pga3dProjectivePoint): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
      x = -r.w * wx,
      y = -r.w * wy,
      z = -r.w * wz,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = (r.y * wx - r.x * wy),
      wxz = (r.z * wx - r.x * wz),
      wyz = (r.z * wy - r.y * wz),
      xyz = 0.0,
      i = 0.0,
    )

  infix def antiGeometric(r: Pga3dRotor): Pga3dRotor =
    Pga3dRotor(
      s = (r.xy * wz + r.yz * wx - r.xz * wy),
      xy = (r.xz * wx + r.yz * wy - r.s * wz),
      xz = (r.s * wy + r.yz * wz - r.xy * wx),
      yz = (-r.s * wx - r.xy * wy - r.xz * wz),
    )

  infix def antiGeometric(r: Pga3dProjectiveTranslator): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (r.wy * wz - r.wz * wy),
      wy = (r.wz * wx - r.wx * wz),
      wz = (r.wx * wy - r.wy * wx),
      xy = -r.s * wz,
      xz = r.s * wy,
      yz = -r.s * wx,
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiGeometric(r: Pga3dTranslator): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (r.wy * wz - r.wz * wy),
      wy = (r.wz * wx - r.wx * wz),
      wz = (r.wx * wy - r.wy * wx),
      xy = -wz,
      xz = wy,
      yz = -wx,
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiGeometric(r: Pga3dVector): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
      x = 0.0,
      y = 0.0,
      z = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = (r.y * wx - r.x * wy),
      wxz = (r.z * wx - r.x * wz),
      wyz = (r.z * wy - r.y * wz),
      xyz = 0.0,
      i = 0.0,
    )

  infix def antiGeometric(r: Pga3dPoint): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
      x = -wx,
      y = -wy,
      z = -wz,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = (r.y * wx - r.x * wy),
      wxz = (r.z * wx - r.x * wz),
      wyz = (r.z * wy - r.y * wz),
      xyz = 0.0,
      i = 0.0,
    )

  infix def antiGeometric(r: Pga3dPlaneCentral): Pga3dMultivector =
    Pga3dMultivector(
      s = 0.0,
      w = 0.0,
      x = (r.y * wz - r.z * wy),
      y = (r.z * wx - r.x * wz),
      z = (r.x * wy - r.y * wx),
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      wxy = 0.0,
      wxz = 0.0,
      wyz = 0.0,
      xyz = (r.x * wx + r.y * wy + r.z * wz),
      i = 0.0,
    )

  infix def antiGeometric(r: Pga3dBivectorBulk): Pga3dRotor =
    Pga3dRotor(
      s = (r.xy * wz + r.yz * wx - r.xz * wy),
      xy = (r.xz * wx + r.yz * wy),
      xz = (r.yz * wz - r.xy * wx),
      yz = (-r.xy * wy - r.xz * wz),
    )

  infix def antiGeometric(r: Pga3dBivectorWeight): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = (r.wy * wz - r.wz * wy),
      wy = (r.wz * wx - r.wx * wz),
      wz = (r.wx * wy - r.wy * wx),
      xy = 0.0,
      xz = 0.0,
      yz = 0.0,
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiGeometric(r: Pga3dPseudoScalar): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = r.i * wx,
      wy = r.i * wy,
      wz = r.i * wz,
    )

  infix def antiGeometric(r: Pga3dPointCenter.type): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = -wx,
      y = -wy,
      z = -wz,
    )

  infix def antiDot(r: Pga3dMotor): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = r.i * wx,
      wy = r.i * wy,
      wz = r.i * wz,
      xy = -r.s * wz,
      xz = r.s * wy,
      yz = -r.s * wx,
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiDot(r: Pga3dPlane): Pga3dProjectivePoint =
    Pga3dProjectivePoint(
      x = -r.w * wx,
      y = -r.w * wy,
      z = -r.w * wz,
      w = (r.x * wx + r.y * wy + r.z * wz),
    )

  infix def antiDot(r: Pga3dBivector): Pga3dPseudoScalar =
    Pga3dPseudoScalar(
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiDot(r: Pga3dProjectivePoint): Pga3dVector =
    Pga3dVector(
      x = (r.y * wz - r.z * wy),
      y = (r.z * wx - r.x * wz),
      z = (r.x * wy - r.y * wx),
    )

  infix def antiDot(r: Pga3dRotor): Pga3dBivectorBulk =
    Pga3dBivectorBulk(
      xy = -r.s * wz,
      xz = r.s * wy,
      yz = -r.s * wx,
    )

  infix def antiDot(r: Pga3dProjectiveTranslator): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = -r.s * wz,
      xz = r.s * wy,
      yz = -r.s * wx,
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiDot(r: Pga3dTranslator): Pga3dMotor =
    Pga3dMotor(
      s = 0.0,
      wx = 0.0,
      wy = 0.0,
      wz = 0.0,
      xy = -wz,
      xz = wy,
      yz = -wx,
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiDot(r: Pga3dVector): Pga3dVector =
    Pga3dVector(
      x = (r.y * wz - r.z * wy),
      y = (r.z * wx - r.x * wz),
      z = (r.x * wy - r.y * wx),
    )

  infix def antiDot(r: Pga3dPoint): Pga3dVector =
    Pga3dVector(
      x = (r.y * wz - r.z * wy),
      y = (r.z * wx - r.x * wz),
      z = (r.x * wy - r.y * wx),
    )

  infix def antiDot(r: Pga3dPlaneCentral): Pga3dProjectivePoint =
    Pga3dProjectivePoint(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
    )

  infix def antiDot(r: Pga3dBivectorWeight): Pga3dPseudoScalar =
    Pga3dPseudoScalar(
      i = (-r.wx * wx - r.wy * wy - r.wz * wz),
    )

  infix def antiDot(r: Pga3dPseudoScalar): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = r.i * wx,
      wy = r.i * wy,
      wz = r.i * wz,
    )

  infix def antiDotI(r: Pga3dBivector): Double =
    (-r.wx * wx - r.wy * wy - r.wz * wz)

  infix def antiDotI(r: Pga3dBivectorWeight): Double =
    (-r.wx * wx - r.wy * wy - r.wz * wz)

  infix def antiWedge(r: Pga3dMotor): Pga3dProjectiveTranslator =
    Pga3dProjectiveTranslator(
      s = (r.xy * wz + r.yz * wx - r.xz * wy),
      wx = r.i * wx,
      wy = r.i * wy,
      wz = r.i * wz,
    )

  inline infix def v(r: Pga3dMotor): Pga3dProjectiveTranslator = antiWedge(r)

  inline infix def join(r: Pga3dMotor): Pga3dProjectiveTranslator = antiWedge(r)

  infix def antiWedge(r: Pga3dBivector): Double =
    (r.xy * wz + r.yz * wx - r.xz * wy)

  inline infix def v(r: Pga3dBivector): Double = antiWedge(r)

  inline infix def join(r: Pga3dBivector): Double = antiWedge(r)

  infix def antiWedge(r: Pga3dProjectivePoint): Pga3dPlane =
    Pga3dPlane(
      x = -r.w * wx,
      y = -r.w * wy,
      z = -r.w * wz,
      w = (r.x * wx + r.y * wy + r.z * wz),
    )

  inline infix def v(r: Pga3dProjectivePoint): Pga3dPlane = antiWedge(r)

  inline infix def join(r: Pga3dProjectivePoint): Pga3dPlane = antiWedge(r)

  infix def antiWedge(r: Pga3dRotor): Double =
    (r.xy * wz + r.yz * wx - r.xz * wy)

  inline infix def v(r: Pga3dRotor): Double = antiWedge(r)

  inline infix def join(r: Pga3dRotor): Double = antiWedge(r)

  infix def antiWedge(r: Pga3dVector): Pga3dPlane =
    Pga3dPlane(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
    )

  inline infix def v(r: Pga3dVector): Pga3dPlane = antiWedge(r)

  inline infix def join(r: Pga3dVector): Pga3dPlane = antiWedge(r)

  infix def antiWedge(r: Pga3dPoint): Pga3dPlane =
    Pga3dPlane(
      x = -wx,
      y = -wy,
      z = -wz,
      w = (r.x * wx + r.y * wy + r.z * wz),
    )

  inline infix def v(r: Pga3dPoint): Pga3dPlane = antiWedge(r)

  inline infix def join(r: Pga3dPoint): Pga3dPlane = antiWedge(r)

  infix def antiWedge(r: Pga3dBivectorBulk): Double =
    (r.xy * wz + r.yz * wx - r.xz * wy)

  inline infix def v(r: Pga3dBivectorBulk): Double = antiWedge(r)

  inline infix def join(r: Pga3dBivectorBulk): Double = antiWedge(r)

  infix def antiWedge(r: Pga3dPseudoScalar): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = r.i * wx,
      wy = r.i * wy,
      wz = r.i * wz,
    )

  inline infix def v(r: Pga3dPseudoScalar): Pga3dBivectorWeight = antiWedge(r)

  inline infix def join(r: Pga3dPseudoScalar): Pga3dBivectorWeight = antiWedge(r)

  infix def antiWedge(r: Pga3dPointCenter.type): Pga3dPlaneCentral =
    Pga3dPlaneCentral(
      x = -wx,
      y = -wy,
      z = -wz,
    )

  inline infix def v(r: Pga3dPointCenter.type): Pga3dPlaneCentral = antiWedge(r)

  inline infix def join(r: Pga3dPointCenter.type): Pga3dPlaneCentral = antiWedge(r)

  infix def cross(r: Pga3dMotor): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (-r.xy * wy - r.xz * wz),
      wy = (r.xy * wx - r.yz * wz),
      wz = (r.xz * wx + r.yz * wy),
    )

  infix def cross(r: Pga3dPlane): Pga3dPlane =
    Pga3dPlane(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
    )

  infix def cross(r: Pga3dBivector): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (-r.xy * wy - r.xz * wz),
      wy = (r.xy * wx - r.yz * wz),
      wz = (r.xz * wx + r.yz * wy),
    )

  infix def cross(r: Pga3dProjectivePoint): Pga3dVector =
    Pga3dVector(
      x = -r.w * wx,
      y = -r.w * wy,
      z = -r.w * wz,
    )

  infix def cross(r: Pga3dRotor): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (-r.xy * wy - r.xz * wz),
      wy = (r.xy * wx - r.yz * wz),
      wz = (r.xz * wx + r.yz * wy),
    )

  infix def cross(r: Pga3dPoint): Pga3dVector =
    Pga3dVector(
      x = -wx,
      y = -wy,
      z = -wz,
    )

  infix def cross(r: Pga3dPlaneCentral): Pga3dPlane =
    Pga3dPlane(
      x = 0.0,
      y = 0.0,
      z = 0.0,
      w = (r.x * wx + r.y * wy + r.z * wz),
    )

  infix def cross(r: Pga3dBivectorBulk): Pga3dBivectorWeight =
    Pga3dBivectorWeight(
      wx = (-r.xy * wy - r.xz * wz),
      wy = (r.xy * wx - r.yz * wz),
      wz = (r.xz * wx + r.yz * wy),
    )

  infix def cross(r: Pga3dPointCenter.type): Pga3dVector =
    Pga3dVector(
      x = -wx,
      y = -wy,
      z = -wz,
    )


object Pga3dBivectorWeight:

  inline val componentsCount = 3

  val zero: Pga3dBivectorWeight = Pga3dBivectorWeight(0.0, 0.0, 0.0)